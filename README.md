
  # Complaint Registration Portal

  This is a code bundle for Complaint Registration Portal. The original project is available at https://www.figma.com/design/eW83gKUErBvR8r5LytaIDU/Complaint-Registration-Portal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  